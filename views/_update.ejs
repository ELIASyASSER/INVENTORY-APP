<!DOCTYPE html>
<html lang="en">
<head>
 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>add products</title>
    <link rel="stylesheet" href="/css/upd.css">
</head>
<body>
    <div class="container">
        
        <h2>Editing :<span><%= myproduct.name %></span></h2>
        <div class="items">
            <form action="/update/product/<%= myproduct._id %>" method="post">
                <label for="product">Product Name</label>
                <input type="text" name="product" id="product" value="<%= myproduct.name %>">
                <label for="category">Product Category</label>

                <select name="category" id="category">
                    <option style="font-weight: bold;color: white;background-color: black;" value="<%=  myproduct.category.id%>"><%= myproduct.category.name%></option>
                    <% Cat.forEach(element => { %>
                        <option  value="<%= element._id %>" ><%=  element.name  %></option>
                    <% }) %>
                </select>

                <label for="price">Product Price</label>
                <input type="number" name="price" id="price" value="<%=Number( myproduct.price) %>">
                
                
                <label for="price">Amount</label>
                <input type="number" name="amount" id="amount" value="<%= myproduct.amount %>">
                <label for="status">Status</label>
                <select name="status" id="status">
                        <option style="font-weight: bold;color: white;background-color: black;" value="<%= myproduct.status %>"><%= myproduct.status %></option>
                    <option value="active">Active</option>
                    <option value="inactive">Inactive</option>
                    <option value="process">Process</option>

                
                </select>
                
                
                
                
                <button class="update" type="submit">Update</button>
            </form>
            <a href="/" class="back">Back Dashboard</a>
        </div>
    </div>
    
</body>
</html>